# schakl — PRODUCTION stack for Portainer, behind YOUR existing Cloudflare Tunnel.
#
# Self-contained (no overlays). Differences vs infra/compose.yaml:
#   • no Traefik, no published ports — your host cloudflared is the only ingress
#   • web + api join the EXTERNAL network your cloudflared stack created, so cloudflared
#     reaches them by name:  /api/*, /mcp/*  → http://api:8000 ;  everything else → http://web:3000
#
# Portainer: create a Stack from this file. Set CF_TUNNEL_NETWORK (+ the secrets below) in the
# stack's Environment variables. The external network must already exist (docker network ls).
name: schakl

x-api-env: &api-env
  SCHAKL_DATABASE_URL: postgresql+asyncpg://${APP_DB_USER:-schakl_app}:${APP_DB_PASSWORD}@db:5432/${POSTGRES_DB:-schakl}
  SCHAKL_REDIS_URL: redis://redis:6379/0
  SCHAKL_SECRET_KEY: ${SCHAKL_SECRET_KEY}
  SCHAKL_BASE_DOMAIN: ${SCHAKL_BASE_DOMAIN}
  SCHAKL_ENVIRONMENT: ${SCHAKL_ENVIRONMENT:-production}
  # Cross-tenant admin surface (issue #26): off by default; instance owners only.
  SCHAKL_INSTANCE_ADMIN_ENABLED: ${SCHAKL_INSTANCE_ADMIN_ENABLED:-false}
  # Daily, unauthenticated GET to the GitHub Releases API; sends nothing about this instance.
  # Set false to disable all outbound update traffic (docs/DEPLOY.md).
  SCHAKL_UPDATE_CHECK_ENABLED: ${SCHAKL_UPDATE_CHECK_ENABLED:-true}
  # --- Single sign-on (#76): configured IN THE APP (Instellingen -> Single sign-on). The
  # SCHAKL_OIDC_* pass-through is retired and kept ONE release only, so the #76 upgrade
  # migration can seed the database from an existing install's env. Remove next release. ---
  SCHAKL_OIDC_ENABLED: ${SCHAKL_OIDC_ENABLED:-false}
  SCHAKL_OIDC_ENFORCED: ${SCHAKL_OIDC_ENFORCED:-false}
  SCHAKL_OIDC_NAME: ${SCHAKL_OIDC_NAME:-sso}
  SCHAKL_OIDC_DISCOVERY_URL: ${SCHAKL_OIDC_DISCOVERY_URL:-}
  SCHAKL_OIDC_CLIENT_ID: ${SCHAKL_OIDC_CLIENT_ID:-}
  SCHAKL_OIDC_CLIENT_SECRET: ${SCHAKL_OIDC_CLIENT_SECRET:-}
  SCHAKL_OIDC_AUTO_PROVISION_MEMBERSHIP: ${SCHAKL_OIDC_AUTO_PROVISION_MEMBERSHIP:-true}
  SCHAKL_OIDC_DEFAULT_ROLE: ${SCHAKL_OIDC_DEFAULT_ROLE:-member}
  # Break-glass (docs/SSO.md): true re-enables local password login regardless of any org's
  # "require SSO" setting. Use it to recover from a broken IdP, then unset it.
  SCHAKL_FORCE_LOCAL_LOGIN: ${SCHAKL_FORCE_LOCAL_LOGIN:-false}

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: ${POSTGRES_ADMIN_USER:-schakl}
      POSTGRES_PASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-schakl}
      APP_DB_USER: ${APP_DB_USER:-schakl_app}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_ADMIN_USER:-schakl} -d ${POSTGRES_DB:-schakl}"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped
    # Declaring `networks:` on ANY service means services that omit it land on `default`
    # instead — a separate network from `schakl`, so `db` would not resolve from `api`.
    networks: [schakl]

  # Creates the NON-superuser application role the API/worker connect as, so Postgres RLS is
  # enforced (superusers bypass it). It owns the public schema so alembic can create tables
  # and policies while still being subject to FORCE ROW LEVEL SECURITY. See CLAUDE.md §5.
  #
  # Deliberately NOT /docker-entrypoint-initdb.d: that only runs when PGDATA is empty, and it
  # needed a host file, which a Portainer web-editor stack cannot provide. This runs on every
  # `up`, is idempotent, and needs nothing on disk.
  db-init:
    image: postgres:16-alpine
    depends_on:
      db:
        condition: service_healthy
    environment:
      PGHOST: db
      PGUSER: ${POSTGRES_ADMIN_USER:-schakl}
      PGPASSWORD: ${POSTGRES_ADMIN_PASSWORD}
      PGDATABASE: ${POSTGRES_DB:-schakl}
      APP_DB_USER: ${APP_DB_USER:-schakl_app}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD}
    # `$$` escapes Compose interpolation so the shell expands these, not Compose.
    entrypoint:
      - sh
      - -ec
      - |
        psql -v ON_ERROR_STOP=1 \
          -v app_user="$$APP_DB_USER" -v app_pass="$$APP_DB_PASSWORD" -v db="$$PGDATABASE" <<'SQL'
        SELECT format('CREATE ROLE %I LOGIN PASSWORD %L NOSUPERUSER', :'app_user', :'app_pass')
        WHERE NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = :'app_user')\gexec

        ALTER ROLE :"app_user" WITH LOGIN PASSWORD :'app_pass' NOSUPERUSER;
        GRANT ALL ON DATABASE :"db" TO :"app_user";
        ALTER SCHEMA public OWNER TO :"app_user";
        GRANT ALL ON SCHEMA public TO :"app_user";
        SQL
        echo "→ app role '$$APP_DB_USER' present and in sync"
    restart: "no"
    networks: [schakl]

  redis:
    image: redis:7-alpine
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20
    restart: unless-stopped
    networks: [schakl]

  api:
    image: ghcr.io/schakl-app/schakl-api:${SCHAKL_TAG:-latest}
    environment: *api-env
    depends_on:
      # Not `db: service_healthy` — the app role must exist before `alembic upgrade head`
      # connects as it. db-init already waits on db being healthy.
      db-init:
        condition: service_completed_successfully
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 15
    restart: unless-stopped
    # Joins the tunnel network ONLY so cloudflared can route /api/* + /mcp/* to it.
    # Publishes no host ports. Remove from `tunnel` (and drop the /api rule in cloudflared)
    # if you don't expose the API on the tunnel.
    networks: [schakl, tunnel]

  worker:
    image: ghcr.io/schakl-app/schakl-api:${SCHAKL_TAG:-latest}
    command: ["worker"]
    environment: *api-env
    depends_on:
      api:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped
    # Reaches db + redis only; never addressed by cloudflared, so not on `tunnel`.
    networks: [schakl]

  web:
    image: ghcr.io/schakl-app/schakl-web:${SCHAKL_TAG:-latest}
    environment:
      SCHAKL_API_URL: http://api:8000
    depends_on:
      api:
        condition: service_healthy
    restart: unless-stopped
    # Internal network (to reach the API) + the tunnel network (cloudflared → http://web:3000).
    networks: [schakl, tunnel]

volumes:
  db-data:

networks:
  schakl:
  # The network your cloudflared stack already created. Set CF_TUNNEL_NETWORK to its real
  # name (docker network ls — usually "<cloudflared-project>_default").
  tunnel:
    external: true
    name: tunnel
