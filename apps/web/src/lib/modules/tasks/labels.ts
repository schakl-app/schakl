/**
 * Task-label palette — re-exports the shared core palette (`core/ui/colors`), kept here so
 * existing imports keep working. The tokens are shared with leave types and calendar chips.
 */

export { LABEL_COLORS, labelChipClass, labelDotClass, type LabelColor } from "$lib/core/ui/colors";
